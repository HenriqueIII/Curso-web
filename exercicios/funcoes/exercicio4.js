// Crie uma função que irá receber dois valores, o dividendo e o divisor. A função deverá imprimir o resultado
// e o resto da divisão destes dois valores.

const divisao = (d, dd) => {
    console.log (`resultado: ${Math.floor(d/dd)}
resto: ${d%dd}`)
}

divisao (11, 2)